import React from 'react'
import Time from './Time';

export const CharacterScreen = () => {
  return (
    <div>
      <h2>Listen</h2>
      {/* <Time /> */}
      {/* Next Step: after User click Start button it is not disapeared, the hole
      screen is changed to CharacterScreen (styles and components for it will be
      later, now return div) Important note: import BtnScreen from
      "./BtnScreen"; I have no file components */}

    </div>
  );
}
